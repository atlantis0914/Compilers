//test return 15410

int main() {
	int ********************************************************************************wibble;
	wibble = alloc(int *******************************************************************************);
	*wibble = alloc(int ******************************************************************************);
	**wibble = alloc(int *****************************************************************************);
	***wibble = alloc(int ****************************************************************************);
	****wibble = alloc(int ***************************************************************************);
	*****wibble = alloc(int **************************************************************************);
	******wibble = alloc(int *************************************************************************);
	*******wibble = alloc(int ************************************************************************);
	********wibble = alloc(int ***********************************************************************);
	*********wibble = alloc(int **********************************************************************);
	**********wibble = alloc(int *********************************************************************);
	***********wibble = alloc(int ********************************************************************);
	************wibble = alloc(int *******************************************************************);
	*************wibble = alloc(int ******************************************************************);
	**************wibble = alloc(int *****************************************************************);
	***************wibble = alloc(int ****************************************************************);
	****************wibble = alloc(int ***************************************************************);
	*****************wibble = alloc(int **************************************************************);
	******************wibble = alloc(int *************************************************************);
	*******************wibble = alloc(int ************************************************************);
	********************wibble = alloc(int ***********************************************************);
	*********************wibble = alloc(int **********************************************************);
	**********************wibble = alloc(int *********************************************************);
	***********************wibble = alloc(int ********************************************************);
	************************wibble = alloc(int *******************************************************);
	*************************wibble = alloc(int ******************************************************);
	**************************wibble = alloc(int *****************************************************);
	***************************wibble = alloc(int ****************************************************);
	****************************wibble = alloc(int ***************************************************);
	*****************************wibble = alloc(int **************************************************);
	******************************wibble = alloc(int *************************************************);
	*******************************wibble = alloc(int ************************************************);
	********************************wibble = alloc(int ***********************************************);
	*********************************wibble = alloc(int **********************************************);
	**********************************wibble = alloc(int *********************************************);
	***********************************wibble = alloc(int ********************************************);
	************************************wibble = alloc(int *******************************************);
	*************************************wibble = alloc(int ******************************************);
	**************************************wibble = alloc(int *****************************************);
	***************************************wibble = alloc(int ****************************************);
	****************************************wibble = alloc(int ***************************************);
	*****************************************wibble = alloc(int **************************************);
	******************************************wibble = alloc(int *************************************);
	*******************************************wibble = alloc(int ************************************);
	********************************************wibble = alloc(int ***********************************);
	*********************************************wibble = alloc(int **********************************);
	**********************************************wibble = alloc(int *********************************);
	***********************************************wibble = alloc(int ********************************);
	************************************************wibble = alloc(int *******************************);
	*************************************************wibble = alloc(int ******************************);
	**************************************************wibble = alloc(int *****************************);
	***************************************************wibble = alloc(int ****************************);
	****************************************************wibble = alloc(int ***************************);
	*****************************************************wibble = alloc(int **************************);
	******************************************************wibble = alloc(int *************************);
	*******************************************************wibble = alloc(int ************************);
	********************************************************wibble = alloc(int ***********************);
	*********************************************************wibble = alloc(int **********************);
	**********************************************************wibble = alloc(int *********************);
	***********************************************************wibble = alloc(int ********************);
	************************************************************wibble = alloc(int *******************);
	*************************************************************wibble = alloc(int ******************);
	**************************************************************wibble = alloc(int *****************);
	***************************************************************wibble = alloc(int ****************);
	****************************************************************wibble = alloc(int ***************);
	*****************************************************************wibble = alloc(int **************);
	******************************************************************wibble = alloc(int *************);
	*******************************************************************wibble = alloc(int ************);
	********************************************************************wibble = alloc(int ***********);
	*********************************************************************wibble = alloc(int **********);
	**********************************************************************wibble = alloc(int *********);
	***********************************************************************wibble = alloc(int ********);
	************************************************************************wibble = alloc(int *******);
	*************************************************************************wibble = alloc(int ******);
	**************************************************************************wibble = alloc(int *****);
	***************************************************************************wibble = alloc(int ****);
	****************************************************************************wibble = alloc(int ***);
	*****************************************************************************wibble = alloc(int **);
	******************************************************************************wibble = alloc(int *);
	*******************************************************************************wibble = alloc(int );
	********************************************************************************wibble = 15410;
	return ********************************************************************************wibble;
}
