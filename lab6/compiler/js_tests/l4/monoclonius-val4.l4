//test return 28

int increment(int *a){
    *a = *a+1;
    return 0;
}
int sum(int a, int b, int c, int d, int e, int f, int g, int h, int i, int j, int k, int l, int m, int n){
    return a + b + c + d + e + f + g + h + i + j + k + l + m + n;
}
int main(){
    int *a = alloc(int);
    *a = 0;
    return sum(increment(a), *a, increment(a), *a, increment(a), *a, increment(a), *a, increment(a),*a, 
                increment(a), *a, increment(a), *a); 
}
