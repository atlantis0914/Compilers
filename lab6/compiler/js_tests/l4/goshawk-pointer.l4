//test return 1337
// pointer fun

int main()
{
    int ************************************************************foo;
    foo = alloc(int ***********************************************************);
    *foo = alloc(int **********************************************************);
    **foo = alloc(int *********************************************************);
    ***foo = alloc(int ********************************************************);
    ****foo = alloc(int *******************************************************);
    *****foo = alloc(int ******************************************************);
    ******foo = alloc(int *****************************************************);
    *******foo = alloc(int ****************************************************);
    ********foo = alloc(int ***************************************************);
    *********foo = alloc(int **************************************************);
    **********foo = alloc(int *************************************************);
    ***********foo = alloc(int ************************************************);
    ************foo = alloc(int ***********************************************);
    *************foo = alloc(int **********************************************);
    **************foo = alloc(int *********************************************);
    ***************foo = alloc(int ********************************************);
    ****************foo = alloc(int *******************************************);
    *****************foo = alloc(int ******************************************);
    ******************foo = alloc(int *****************************************);
    *******************foo = alloc(int ****************************************);
    ********************foo = alloc(int ***************************************);
    *********************foo = alloc(int **************************************);
    **********************foo = alloc(int *************************************);
    ***********************foo = alloc(int ************************************);
    ************************foo = alloc(int ***********************************);
    *************************foo = alloc(int **********************************);
    **************************foo = alloc(int *********************************);
    ***************************foo = alloc(int ********************************);
    ****************************foo = alloc(int *******************************);
    *****************************foo = alloc(int ******************************);
    ******************************foo = alloc(int *****************************);
    *******************************foo = alloc(int ****************************);
    ********************************foo = alloc(int ***************************);
    *********************************foo = alloc(int **************************);
    **********************************foo = alloc(int *************************);
    ***********************************foo = alloc(int ************************);
    ************************************foo = alloc(int ***********************);
    *************************************foo = alloc(int **********************);
    **************************************foo = alloc(int *********************);
    ***************************************foo = alloc(int ********************);
    ****************************************foo = alloc(int *******************);
    *****************************************foo = alloc(int ******************);
    ******************************************foo = alloc(int *****************);
    *******************************************foo = alloc(int ****************);
    ********************************************foo = alloc(int ***************);
    *********************************************foo = alloc(int **************);
    **********************************************foo = alloc(int *************);
    ***********************************************foo = alloc(int ************);
    ************************************************foo = alloc(int ***********);
    *************************************************foo = alloc(int **********);
    **************************************************foo = alloc(int *********);
    ***************************************************foo = alloc(int ********);
    ****************************************************foo = alloc(int *******);
    *****************************************************foo = alloc(int ******);
    ******************************************************foo = alloc(int *****);
    *******************************************************foo = alloc(int ****);
    ********************************************************foo = alloc(int ***);
    *********************************************************foo = alloc(int **);
    **********************************************************foo = alloc(int *);
    ***********************************************************foo = alloc(int );
    ************************************************************foo = 1337;
    return ************************************************************foo;
}
