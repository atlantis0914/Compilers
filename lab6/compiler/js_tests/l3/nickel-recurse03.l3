//test return 5
// iterated log function
int log(int n)
{
	int count = 0;
	while




(n > 1)
	{
		n /= 2;
		count           /* lolz */        ++;
	}
	return count;
}

int itlog(int n)
{
	return n <= 1 ? 1 : 1 + itlog(log(n));
}

int main()
{
	return itlog(0x7FFFFFFF);
}
