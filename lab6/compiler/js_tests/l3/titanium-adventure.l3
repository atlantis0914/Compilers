//test return 15
// Calculates the 5th triangle number.

int triangle(int n) {
  int i = 0;
  int count = 0;
  for (i = 1; i <= n; i++) {
    count += i;
  }
  return count;
}

int main() {
  return triangle(5);
}
