//test error
// Test compiler error for reserved keywords
// Really needs a separate test for each keyword, since 
// we can't easily detect if a single one of these doesn't
// cause an error.
int main() {
    int int;
    int char;
    int string;
    int void;
    int while;
    int for;
    int if;
    int return;
    int break;
    int continue;
    int NULL;
    int alloc;
    int alloc_array;
    int typedef;
    int struct;

    int=1;
    char=1;
    string=1;
    void=1;
    while=1;
    for=1;
    if=1;
    return=1;
    break=1;
    continue=1;
    NULL=1;
    alloc=1;
    alloc_array=1;
    typedef=1;
    struct=1;
}
