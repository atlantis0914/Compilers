//test return 1

int sum1(int x) {
    int total = 0;

    for(int i = 0; i <= x; i++)
      total += i;

    return(total);
}

int sum2(int x) {
    return (x*(x+1))/2;
}

int main() {
    if(sum1(1000) == sum2(1000))
      return 1;
    return 0;
}
