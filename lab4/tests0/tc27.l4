//test error
/* recursively defined struct */

struct tree;

struct list {
  struct tree head;
  struct list * tail;
};

struct tree {
  int data;
  struct list subtrees;
};

int main () {
  return -1;
}
