//test return 4

// file with all arithmetic operators

int main() {
    int x = 6;
    int y = 3;
    int z = 7;

    x += y - z;
    y -= z * z + 6;
    z *= x / y;
    x /= z % y + 1;
    x %= y + x - 1;

    return x + 2;
}
