//test return 75
/* Testing external function calls to
 * floating point operations
 */

int main () {
  fpt x = itof(10);
  x = fmul(x,fdiv(fadd(x,itof(5)),itof(2)));
  print_fpt(x);
  return ftoi(x);
}
