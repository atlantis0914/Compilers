//test exception 8
// division by 0
int main()
{
    int x = 0;      // 0
    int y = x + 1;  // 1
    int z = y + 1;  // 2
    int w = z + 1;  // 3
    if ( (x < y) && (y < z) && (z < w) && (w < x) ) {
        return 0;
    }
    return 1 / 0;
}
