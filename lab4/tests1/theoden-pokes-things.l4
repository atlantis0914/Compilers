//test return 42

// poke!

int main()
{
  int ****************************************************************p;

  p = alloc(int ***************************************************************);
  *p = alloc(int **************************************************************);
  **p = alloc(int *************************************************************);
  ***p = alloc(int ************************************************************);
  ****p = alloc(int ***********************************************************);
  *****p = alloc(int **********************************************************);
  ******p = alloc(int *********************************************************);
  *******p = alloc(int ********************************************************);
  ********p = alloc(int *******************************************************);
  *********p = alloc(int ******************************************************);
  **********p = alloc(int *****************************************************);
  ***********p = alloc(int ****************************************************);
  ************p = alloc(int ***************************************************);
  *************p = alloc(int **************************************************);
  **************p = alloc(int *************************************************);
  ***************p = alloc(int ************************************************);
  ****************p = alloc(int ***********************************************);
  *****************p = alloc(int **********************************************);
  ******************p = alloc(int *********************************************);
  *******************p = alloc(int ********************************************);
  ********************p = alloc(int *******************************************);
  *********************p = alloc(int ******************************************);
  **********************p = alloc(int *****************************************);
  ***********************p = alloc(int ****************************************);
  ************************p = alloc(int ***************************************);
  *************************p = alloc(int **************************************);
  **************************p = alloc(int *************************************);
  ***************************p = alloc(int ************************************);
  ****************************p = alloc(int ***********************************);
  *****************************p = alloc(int **********************************);
  ******************************p = alloc(int *********************************);
  *******************************p = alloc(int ********************************);
  ********************************p = alloc(int *******************************);
  *********************************p = alloc(int ******************************);
  **********************************p = alloc(int *****************************);
  ***********************************p = alloc(int ****************************);
  ************************************p = alloc(int ***************************);
  *************************************p = alloc(int **************************);
  **************************************p = alloc(int *************************);
  ***************************************p = alloc(int ************************);
  ****************************************p = alloc(int ***********************);
  *****************************************p = alloc(int **********************);
  ******************************************p = alloc(int *********************);
  *******************************************p = alloc(int ********************);
  ********************************************p = alloc(int *******************);
  *********************************************p = alloc(int ******************);
  **********************************************p = alloc(int *****************);
  ***********************************************p = alloc(int ****************);
  ************************************************p = alloc(int ***************);
  *************************************************p = alloc(int **************);
  **************************************************p = alloc(int *************);
  ***************************************************p = alloc(int ************);
  ****************************************************p = alloc(int ***********);
  *****************************************************p = alloc(int **********);
  ******************************************************p = alloc(int *********);
  *******************************************************p = alloc(int ********);
  ********************************************************p = alloc(int *******);
  *********************************************************p = alloc(int ******);
  **********************************************************p = alloc(int *****);
  ***********************************************************p = alloc(int ****);
  ************************************************************p = alloc(int ***);
  *************************************************************p = alloc(int **);
  **************************************************************p = alloc(int *);
  ***************************************************************p = alloc(int );

  ****************************************************************p = 42;

  return ****************************************************************p;
}
