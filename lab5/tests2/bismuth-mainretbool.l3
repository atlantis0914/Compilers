//test error
/* Computes carmichael numbers
 * a number is a carmichael number if it is a positive integer n
 * b^n = b (mod n) for all integers 1 < b < n-1
 */

bool is_carmichael(int n)
{
  if (n < 3)
    return false;

  return true;
}

bool main ()
{
  return is_carmichael(3);
}
