//test return 35
// A function with more than 6 args (passed on the stack)

int fun(int a, int b, int c, int d, int e, int f, int g, int h) {
    return (a + b*c - d/e)*(f + g - h);
}

int main() {
    return fun(1, 2, 3, 4, 5, 6, 7, 8);
}

