//test return 1
// x is a perfect square if x = n*n for some int n

bool isperfect(int x) {
    for(int i = 0; i <= x; i++) {
        if(x == i*i) return true;
    }
    return false;
}

int main() {
    if(isperfect(625)) return 1;
    return 0;
}

