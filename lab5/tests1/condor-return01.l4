//test return 0
/* a test file to make sure basic structs are working */
struct HelloStruct {
    int hour;
    bool is_morning;
};

int main () {
   struct HelloStruct * hi = alloc( struct HelloStruct );
   hi->hour = 0;
   hi->is_morning = false;
   return hi->hour;
}