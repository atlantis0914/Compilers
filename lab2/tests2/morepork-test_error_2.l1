//test exception 8

/* 
 * This is my test program
 * Division of INT_MIN by -1
 * should result in overflow
 */
int main () {
  int x;
  int y = 15;
  ( x ) = -2147483648;
  y = -1;
  x = x / y;
  y = -y;
  x += 1;
  //should return 4
  return ( x + y);
}
