//test error

/* 
 * This is my test program
 * Using uninitialized variable
 */
int main () {
  int x;
  int y = 15;
  ( y ) = x;
  x = 2 + 7;
  y = y - ( x );
  y = -y;
  x += 1;
  //should return 4
  return ( x + y);
}
