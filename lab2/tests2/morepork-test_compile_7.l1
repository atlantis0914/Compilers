//test error

/* 
 * This is my test program
 * Testing reserved word
 */
int main () {
  int x;
  int y = 15;
  ( x ) = 0;
  x = struct;
  y = y - ( x );
  y = -y;
  x += 1;
  //should return 4
  return ( x + y);
}
