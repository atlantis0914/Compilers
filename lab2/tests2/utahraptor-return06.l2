//test return 1
// more conditional tests
int main()
{
	int x = 1996;
	bool xleap = (x % 4 == 0 && (x % 100 == 0 ? x % 400 == 0 : true));

	int y = 1900;
	bool yleap = (y % 4 == 0 && (y % 100 == 0 ? y % 400 == 0 : true));

	int z = 2000;
	bool zleap = (z % 4 == 0 && (z % 100 == 0 ? z % 400 == 0 : true));

	if (xleap && !yleap && zleap)
		return 1;
	return 0;
}
