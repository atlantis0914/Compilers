//test return 144
//computes fibonacci number
int main()
{
  int x = 1;
  int y = 1;
  x = x + y;
  y = x + y;
  x = x + y;
  y = x + y;
  x = x + y;
  y = x + y;
  x = x + y;
  y = x + y;
  x = x + y;
  y = x + y;

  return y;
}
