//test return 474
// Ternary funtime.
int main() {
  int x = 1000;
  int r = 100;
  int s = 10;
  for (int i = 0; i < 1000; i++) {
    r = r > 0 ? r - 10 : r + 1;
    x = x >> 2 < 100 ? x << 1 : x / 3;
    s = s > r ? (s > x ? 1 : s * 12) : s * 6;
  }
  return r + x + s;
}
