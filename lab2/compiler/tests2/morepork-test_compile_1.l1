//test error

/* 
 * This is my test program
 * Missing ident for -
 */
int main () {
  int x;
  int y = 15;
  ( x ) = 0;
  x = 2 + 7;
  y = y - ( x );
  y = -;
  x += 1;
  //should return 4
  return ( x + y);
}
